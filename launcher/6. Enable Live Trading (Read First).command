#!/bin/bash
# Live trading is outside the supported product boundary. This non-mutating
# stub replaces the former credential-collection and mode-switching launcher.

echo ""
echo "  Live trading is disabled and cannot be enabled by a launcher."
echo "  This programme supports mock and explicitly gated demo validation only."
echo "  No configuration or credentials were changed."
echo ""
exit 2
